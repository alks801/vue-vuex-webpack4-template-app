import Api from '../../src/api'
import { expect } from 'chai'
import flushPromises from 'flush-promises'

describe('Weather.js', () => {
   /*  it('getWeather endpoint returns not null array', () => {
        //let result = Api.getWeather()
        // await flushPromises()
        expect(result.data).not.to.be.empty
    }) */

    /* it('addNewCity endpoint return correct status code', () => {
        expect(WeatherStore.getters.allWeather).to.equal([])
    }) */
})