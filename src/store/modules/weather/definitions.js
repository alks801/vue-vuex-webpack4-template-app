const getters = {
    ALL_WEATHER: 'allWeather'
}

const actions = {
    GET_ALL_WEATHER: 'getAllWeather',
    ADD_WEATHER: 'addWeather'
}

const mutations = {
    SET_WEATHER: 'setWeather',
    ADD_WEATHER: 'addWeather'
}

export default {
    getters,
    actions,
    mutations
}